services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: vedant
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: de_project
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: